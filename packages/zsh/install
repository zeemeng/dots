#!/usr/bin/env sh

if [ "$(uname -s)" = "Darwin" ]; then
	"$CONFMAN_LOG" warning 'Skipping installation with Homebrew on macOS, prefer the system-provided Zsh to avoid. Avoids issues with compinit..'
else
	# Default install otherwise
	"$CONFMAN_TASK" install
fi

